<?xml version="1.0" encoding="utf-8" ?>

<ContentPage
    x:Class="LugarLang.Mobile.Pages.CategoryManagementPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Manage Categories">

    <VerticalStackLayout
        Padding="20"
        Spacing="16">

        <Label
            Text="Manage Categories"
            FontSize="28"
            FontAttributes="Bold" />

        <Button
            Text="+ Add Category"
            Clicked="OnAddCategoryClicked" />

        <CollectionView
            x:Name="CategoryCollectionView">

            <CollectionView.ItemTemplate>

                <DataTemplate>

                    <Grid
                        Padding="8"
                        ColumnDefinitions="*,Auto,Auto"
                        ColumnSpacing="8">

                        <Label
    Text="{Binding Name}"
    FontSize="18"
    VerticalOptions="Center">

                            <Label.Triggers>

                                <DataTrigger
            TargetType="Label"
            Binding="{Binding IsEnabled}"
            Value="False">

                                    <Setter
                Property="TextColor"
                Value="Gray" />

                                </DataTrigger>

                            </Label.Triggers>

                        </Label>
                        
                        

                        <Button
                            Grid.Column="1"
                            Text="✎"
                            Clicked="OnEditCategoryClicked" />

                        <Button
                            Grid.Column="2"
                            Text="🗑"
                            Clicked="OnDeleteCategoryClicked" />

                    </Grid>

                </DataTemplate>

            </CollectionView.ItemTemplate>

        </CollectionView>

    </VerticalStackLayout>

</ContentPage>